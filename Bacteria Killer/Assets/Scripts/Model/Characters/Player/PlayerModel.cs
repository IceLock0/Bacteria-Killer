using Configs;

namespace Model.Characters.Player
{
    public class PlayerModel : CharacterModel
    {
        public PlayerModel (PlayerConfig playerConfig) : base(playerConfig)
        {
            
        }
        
    }
}